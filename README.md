# CitySense Dashboard Mockup

**CitySense Dashboard** คือ Mockup เว็บแอปพลิเคชันสำหรับแสดงผลข้อมูลการแจ้งเหตุร้องเรียนของหน่วยงานในเมือง (เช่น สำนักการระบายน้ำ, จราจร) บนแผนที่แบบ Interactive โดยเน้นการใช้งานที่ง่าย รวดเร็ว และรองรับการแสดงผลบนมือถือ (**Mobile-First Design**)
https://gemini.google.com/app/4add27e5cb367f02

---

## 💻 Tech Stack & Libraries

โปรเจกต์นี้พัฒนาโดยใช้เทคโนโลยีและไลบรารีดังต่อไปนี้:

| หมวดหมู่ | เทคโนโลยีที่ใช้ | รายละเอียด |
| --- | --- | --- |
| **Core Framework** | React (v18+) | ไลบรารีหลักสำหรับสร้าง User Interface |
| **Build Tool** | Vite | เครื่องมือ Build ที่รวดเร็วและเบา |
| **Styling** | Tailwind CSS | Utility-first CSS framework สำหรับทำ Responsive Design |
| **Map Engine** | Leaflet.js | ไลบรารีแผนที่ Open-source ที่มีความยืดหยุ่นสูง |
| **Icons** | Lucide React | ชุดไอคอนที่สวยงามและปรับแต่งได้ง่าย |
| **Deployment** | GitHub Pages | สำหรับโฮสต์หน้าเว็บ Demo |

---

## 📌 ภาพรวมฟีเจอร์และการแสดงผล

Mockup นี้ถูกออกแบบมาเพื่อจำลองสถานการณ์การใช้งานจริงของเจ้าหน้าที่หรือผู้บริหารเมือง โดยมีส่วนประกอบหลักดังนี้:

### 1. แผนที่อัจฉริยะ (Interactive Map)

* **ตำแหน่ง:** พื้นที่หลักของหน้าจอ (ขวา)
* **เทคโนโลยี:** ใช้ Leaflet.js แสดงแผนที่จริง
* **โหมดการแสดงผล:**
* 📍 **Marker:** หมุดแสดงจุดเกิดเหตุ สีของหมุดบ่งบอกสถานะ (แดง=รอดำเนินการ, เหลือง=กำลังทำ, เขียว=เสร็จสิ้น)
* 🔴 **Cluster:** รวมกลุ่มหมุดที่อยู่ใกล้กันเป็นวงกลมพร้อมตัวเลขจำนวน (ช่วยลดความรกของหน้าจอ)
* 🔥 **Heatmap:** แสดงความหนาแน่นของจุดแจ้งเหตุด้วยเฉดสี (เหมาะสำหรับดูพื้นที่ที่มีปัญหาซ้ำซาก)


* **เป้าหมาย:** เพื่อให้เห็นภาพรวมเชิงพื้นที่ (Spatial Context) ได้ทันที

### 2. แถบควบคุมและกรองข้อมูล (Sidebar & Filters)

* **ตำแหน่ง:** ด้านซ้าย (Desktop) หรือ Slide-over เต็มจอ (Mobile)
* **ฟีเจอร์:**
* **Search:** ค้นหาด้วย Keyword หรือรหัสเรื่อง
* **Status Filter:** กรองตามสถานะงาน (เช่น "ของบประมาณ")
* **Category Filter:** กรองตามประเภทเรื่อง (น้ำท่วม, ไฟดับ ฯลฯ)
* **Rating Filter:** กรองตามระดับความพึงพอใจ


* **UX Design:** ใช้ระบบ **"Batch Filtering"** (เลือกเงื่อนไขให้ครบแล้วกดปุ่ม "ค้นหา") เพื่อลดภาระ Server

### 3. รายละเอียดเรื่องแจ้ง (Detail Popup)

* **ตำแหน่ง:** แสดงขึ้นมาจากด้านล่างขวา (Desktop) หรือด้านล่างจอ (Mobile) เมื่อกดที่หมุด
* **ข้อมูล:** รูปภาพหน้างาน, รหัส, สถานะปัจจุบัน, คำอธิบาย, และระดับความพึงพอใจ
* **เป้าหมาย:** เพื่อให้ดูรายละเอียดเจาะลึกได้โดยไม่ต้องเปลี่ยนหน้า (Single Page Application feel)

---

## 🛠 วิธีนำมาพัฒนาต่อในเครื่อง (Local Development)

### สิ่งที่ต้องมี

* Node.js (แนะนำ v18 ขึ้นไป)
* Git

### ขั้นตอนการติดตั้ง

1. **Clone Repository:**
```bash
git clone https://github.com/<YOUR_USERNAME>/mockup-internal-map.git
cd mockup-internal-map

```


2. **ติดตั้ง Library:**
```bash
npm install

```


3. **รันโปรเจกต์:**
```bash
npm run dev

```


จากนั้นเปิดไปที่ `http://localhost:5173` เพื่อดูผลลัพธ์

---

## 🚀 วิธีนำขึ้นแสดงผลบน GitHub Pages (Deployment)

1. **ตั้งค่า `vite.config.js**`
ตรวจสอบให้แน่ใจว่าค่า `base` ตรงกับชื่อ Repository ของคุณ:
```javascript
export default defineConfig({
  plugins: [react()],
  base: '/mockup-internal-map/', // <--- ต้องตรงกับชื่อ repo ของคุณ
})

```


2. **สั่ง Deploy**
รันคำสั่งใน Terminal:
```bash
npm run deploy

```


*สคริปต์จะทำการ Build และส่งไฟล์ไปยัง branch `gh-pages` โดยอัตโนมัติ*
3. **เปิดใช้งานที่ GitHub**
* ไปที่เมนู **Settings > Pages** ใน Repository ของคุณ
* เลือก Branch เป็น `gh-pages` และโฟลเดอร์ `/ (root)`
* รอสักครู่ ลิงก์จะปรากฏที่ด้านบน (เช่น `https://<user>.github.io/mockup-internal-map/`)



---

## 📂 โครงสร้างไฟล์สำคัญ

* `src/AgencyMapDashboard.jsx`: ไฟล์หลักที่รวม Logic แผนที่, การกรองข้อมูล, และ UI ทั้งหมด
* `src/App.jsx`: ตัวเรียกใช้งาน Dashboard
* `tailwind.config.js`: การตั้งค่า Theme ของ CSS

---

**คุณต้องการให้ผมช่วยเขียนคำอธิบายส่วนไหนในโค้ดเพิ่มเติม หรืออยากให้เพิ่มส่วน "คำถามที่พบบ่อย" (FAQ) ลงไปในไฟล์นี้ด้วยไหมครับ?**